node_modules
.next
out
.vercel
# Every env form, not just *.local. A bare `.env` and `.env.production` were
# NOT covered before, so the first one anyone created would have been committed.
.env
.env.*
!.env.example


.vercel

# Design review captures. Regenerate with `npm run capture`.
design-baseline/

# Auto-generated by Next.js on every build.
AGENTS.md
CLAUDE.md

# Local MCP dev config, not part of the app.
.mcp.json

# Local Python service (deployed separately to Render).
AI-Defense-Lab*/

# Reference footage, kept locally.
/this.mp4

# Credential material. Nothing here has ever been committed; this keeps it that way.
*.pem
*.key
*.p12
*.pfx
.npmrc
.netrc
id_rsa*
